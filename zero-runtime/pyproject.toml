[project]
name = "zero-runtime"
version = "0.1.0"
description = "Kernel-side runtime shim for Zero recursion"
requires-python = ">=3.10"
dependencies = [
  "ipykernel",
  "nest-asyncio",
  "tyro",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rlm"]
